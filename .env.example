DATABASE_URL=
# VITE_ASSETS_URL is no longer required.
# Uploaded files are stored in public/local-assets/uploads/ inside the project
# and served directly by the dev server at /local-assets/uploads/...
